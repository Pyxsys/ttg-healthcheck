matrix:
  include:
    - language: node_js
      node_js:
        - "stable"
      before_script:
        - cd client
        - npm install
      script:
        - npm run test
    - language: node_js
      node_js:
        - "stable"
      before_script:
        - cd server
        - npm install
      script:
        - npm run test
